function calculate(num1: number, num2: number) {
  const operation1 = num1 + num2;
  const operation2 = operation1 * 25;
  const operation3 = operation2 / 5;

  return operation3;
}

function calculate2(num1: number, num2: number) {
  const operation1 = num1 + num2;
  const operation2 = operation1 * 25;
  const operation3 = operation2 / 5;

  return operation3 + 10;
}
